name: Build Blender Extension

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build extension ZIP
        shell: pwsh
        run: tools/build_release.ps1

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: scene_qc_validator_by_mutaform_studio
          path: dist/scene_qc_validator_by_mutaform_studio.zip

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: dist/scene_qc_validator_by_mutaform_studio.zip
